// import * as types from '../mutation-types'
import * as actions from './actions'
import * as getters from './getters'
// initial state
const state = {
}

// mutations
const mutations = {
}

export default {
  state,
  mutations,
  getters,
  actions
}
